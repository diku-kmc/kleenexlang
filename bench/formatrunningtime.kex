/* Kleenex script for formatting running time files.

The format of a "running time file" as generated by the runningtime.sh script
is the stderr output of running a Kleenex program with the -t option on.
*/

runningTime := line <\n> runningTime
             | line <\n>

// Throw away the human info strings.
line := ~<time[^0-9\n]*> <[0-9]+>