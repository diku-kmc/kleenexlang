/* Strips comments from INI files */
stripini := comment stripini
          | blank   stripini
          | <[^\n]*\n> stripini
          | comment
          | blank
          | <[^\n]*\n>

comment := skipWs ~<;[^\n]*>

blank := skipWs ~<\n>

skipWs := ~<[ \t]*>
